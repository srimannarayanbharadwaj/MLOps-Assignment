# Processed Data

`heart_cleveland_clean.csv` is generated by:

```powershell
python -m src.data_download
```

The cleaning step replaces UCI `?` missing values, imputes the small number of missing `ca` and `thal` values with the mode, and converts the target into a binary label.

